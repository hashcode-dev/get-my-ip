kind = "web"
previewPath = "/"
title = "IP Finder"
version = "1.0.0"
id = "artifacts/ip-finder"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19469

[services.development]
run = "pnpm --filter @workspace/ip-finder run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ip-finder", "run", "build" ]
publicDir = "artifacts/ip-finder/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19469"
BASE_PATH = "/"
